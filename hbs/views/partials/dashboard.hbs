<div class="dashboard-grid">
    <!-- Sidebar -->
    <div class="dashboard-sidebar">
        <div class="user-card panel">
            <h3>{{user.username}}</h3>
            <p class="user-name">{{user.firstName}} {{user.lastName}}</p>
            <p class="user-email">{{user.email}}</p>
            <p class="user-age"><i class="fas fa-calendar-alt"></i> {{user.age}} let</p>
            
            <!-- Zemljevid za lokacijo -->
            <div class="user-location-map">
                <div id="userMap" style="height: 180px; width: 100%; border-radius: 16px; margin: 10px 0;"></div>
                <div class="location-info">
                    <i class="fas fa-map-marker-alt"></i> 
                    <span>Radij: {{user.location.radius}} km</span>
                </div>
            </div>
            
            <div class="search-status" id="searchStatus">
                {{#if user.activeSearch}}
                    <i class="fas fa-bell"></i> Iskanje aktivno
                {{else}}
                    <i class="fas fa-bell-slash"></i> Iskanje ni aktivno
                {{/if}}
            </div>
            <form id="activateSearchForm">
                <button type="submit" class="btn-primary btn-full">
                    <i class="fas fa-search"></i> Aktiviraj iskanje
                </button>
            </form>
            <button onclick="window.location.href='/profile/edit'" class="btn-secondary btn-full" style="margin-top: 0.5rem;">
                <i class="fas fa-edit"></i> Uredi profil
            </button>
        </div>
        
        <div class="interests-card panel">
            <h4><i class="fas fa-heart"></i> Moji interesi</h4>
            <div class="interests-tags">
                {{#each user.interests}}
                    <span class="interest-tag">{{this}}</span>
                {{/each}}
            </div>
        </div>
        
        <div class="availability-card panel">
            <h4><i class="fas fa-clock"></i> Razpoložljivost</h4>
            <!-- IDENTIČEN KOLEDAR KOT V REGISTRACIJI -->
            <div class="calendar-grid">
                <div class="calendar-header"></div>
                {{#each weekdays}}
                <div class="calendar-header day-header">{{this}}</div>
                {{/each}}
                
                {{#each dayPartsWithTimes}}
                <div class="time-label">
                    {{this.label}}<br>
                    <span class="time-range">{{this.range}}</span>
                </div>
                {{#each ../weekdays}}
                <div class="calendar-slot-display" data-day="{{this}}" data-part="{{../this.key}}">
                    <span class="slot-time">{{../this.range}}</span>
                </div>
                {{/each}}
                {{/each}}
            </div>
            <div class="calendar-note">* Obarvani termini so tvoji prosti časi.</div>
        </div>
    </div>
    
    <!-- Main Content -->
    <div class="dashboard-main">
        <div class="suggestions-header">
            <h2><i class="fas fa-users"></i> Predlogi skupin</h2>
            <p>Na podlagi tvojih interesov in razpoložljivosti</p>
        </div>
        <div id="groupSuggestionsContainer" class="suggestions-grid">
            {{#each suggestions}}
                <div class="group-card">
                    <div class="group-header">
                        <span class="group-badge">{{name}}</span>
                        <span class="match-badge">{{matchScore}}% ujemanja</span>
                    </div>
                    <div class="group-members">
                        {{#each members}}
                            <span class="member-chip">{{this}}</span>
                        {{/each}}
                    </div>
                    <div class="group-details">
                        <span><i class="fas fa-tag"></i> Skupni interesi: {{interests.join ", "}}</span>
                    </div>
                    <div class="group-details">
                        <span><i class="fas fa-clock"></i> {{time}}</span>
                        <span><i class="fas fa-map-marker-alt"></i> {{location}}</span>
                    </div>
                    <div class="group-actions">
                        <form action="/api/meetings/confirm" method="POST">
                            <input type="hidden" name="userId" value="{{../user._id}}">
                            <input type="hidden" name="groupName" value="{{name}}">
                            <input type="hidden" name="members" value="{{json members}}">
                            <input type="hidden" name="location" value="{{location}}">
                            <input type="hidden" name="dateTime" value="{{time}}">
                            <button type="submit" class="btn-primary"><i class="fas fa-check-circle"></i> Sprejmem predlog</button>
                        </form>
                        <button class="btn-secondary" onclick="alert('Klepet bo na voljo kmalu!')"><i class="fas fa-comment-dots"></i> Pogovor</button>
                    </div>
                </div>
            {{else}}
                <div class="group-card" style="text-align: center; padding: 3rem;">
                    <i class="fas fa-search" style="font-size: 3rem; color: #cbd5e1; margin-bottom: 1rem;"></i>
                    <h3>Trenutno ni predlogov</h3>
                    <p style="color: #64748b;">Aktiviraj iskanje za prejemanje predlogov skupin.</p>
                </div>
            {{/each}}
        </div>
        
        <!-- Confirmed Meetings -->
        <div class="confirmed-meetings-section" style="margin-top: 2rem;">
            <div class="suggestions-header">
                <h2><i class="fas fa-check-circle"></i> Moja potrjena srečanja</h2>
                <p>Srečanja, ki si jih potrdil/a</p>
            </div>
            <div class="suggestions-grid">
                {{#each confirmedMeetings}}
                    <div class="group-card">
                        <div class="group-header">
                            <span class="group-badge">{{groupName}}</span>
                            <span class="confirmed-badge">Potrjeno</span>
                        </div>
                        <div class="meeting-participants">
                            <strong><i class="fas fa-users"></i> Udeleženci:</strong><br>
                            {{#each members}}
                                <span class="participant-name">{{this}}</span>
                            {{/each}}
                        </div>
                        <div class="group-details">
                            <span><i class="fas fa-clock"></i> {{dateTime}}</span>
                            <span><i class="fas fa-map-marker-alt"></i> {{location}}</span>
                        </div>
                    </div>
                {{else}}
                    <div class="group-card" style="text-align: center; padding: 3rem;">
                        <i class="fas fa-clock" style="font-size: 3rem; color: #cbd5e1; margin-bottom: 1rem;"></i>
                        <h3>Ni potrjenih srečanj</h3>
                        <p style="color: #64748b;">Ko sprejmeš predlog skupine, se bo prikazal tukaj.</p>
                    </div>
                {{/each}}
            </div>
        </div>
    </div>
</div>

<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script>
<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />

<script>
    // Razpoložljivost uporabnika
    const userAvailability = {{{json user.availability}}};
    
    // Obarvaj celice v koledarju (IDENTIČNO kot v registraciji)
    function markAvailability() {
        const slots = document.querySelectorAll('.calendar-slot-display');
        
        slots.forEach(slot => {
            const day = slot.getAttribute('data-day');
            const part = slot.getAttribute('data-part');
            const value = `${day}__${part}`;
            
            if (userAvailability && userAvailability.includes(value)) {
                slot.classList.add('selected-slot');
            }
        });
    }
    
    // Zemljevid za uporabnikovo lokacijo
    function initUserMap() {
        const lat = {{user.location.lat}};
        const lng = {{user.location.lng}};
        const radius = {{user.location.radius}};
        
        const mapContainer = document.getElementById('userMap');
        if (!mapContainer) return;
        
        if (lat && lng && lat !== 0 && lng !== 0 && lat !== null && lng !== null) {
            try {
                const map = L.map('userMap').setView([lat, lng], 12);
                
                L.tileLayer('https://{s}.basemaps.cartocdn.com/light_all/{z}/{x}/{y}{r}.png', {
                    attribution: '&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a>',
                    subdomains: 'abcd',
                    maxZoom: 19
                }).addTo(map);
                
                L.marker([lat, lng]).addTo(map).bindPopup('Tvoja lokacija').openPopup();
                
                L.circle([lat, lng], {
                    color: '#818cf8',
                    fillColor: '#c084fc',
                    fillOpacity: 0.2,
                    radius: radius * 1000
                }).addTo(map);
            } catch (err) {
                mapContainer.innerHTML = '<div style="height: 100%; display: flex; align-items: center; justify-content: center; background: #1e293b; border-radius: 16px; color: #64748b;">Napaka pri nalaganju zemljevida</div>';
            }
        } else {
            mapContainer.innerHTML = '<div style="height: 100%; display: flex; align-items: center; justify-content: center; background: #1e293b; border-radius: 16px; color: #64748b;">Ni izbrane lokacije</div>';
        }
    }
    
    // Aktivacija iskanja
    const activateForm = document.getElementById('activateSearchForm');
    if (activateForm) {
        activateForm.addEventListener('submit', async function(e) {
            e.preventDefault();
            const userId = '{{user._id}}';
            
            try {
                const response = await fetch(`/api/auth/activate-search/${userId}`, {
                    method: 'PUT',
                    headers: { 'Content-Type': 'application/json' }
                });
                
                if (response.ok) {
                    location.reload();
                } else {
                    const data = await response.json();
                    alert('Napaka: ' + (data.message || 'Aktivacija ni uspela'));
                }
            } catch (err) {
                alert('Napaka: ' + err.message);
            }
        });
    }
    
    // Inicializacija
    document.addEventListener('DOMContentLoaded', function() {
        markAvailability();
        initUserMap();
    });
</script>

<style>
    /* Dashboard sidebar - brez avatarja */
    .user-card {
        text-align: center;
    }
    .user-name {
        font-size: 0.9rem;
        color: #94a3b8;
        margin-bottom: 0.3rem;
    }
    .user-email {
        font-size: 0.8rem;
        color: #64748b;
        margin-bottom: 0.3rem;
    }
    .user-age {
        font-size: 0.85rem;
        color: #94a3b8;
        margin-bottom: 0.5rem;
    }
    .user-location-map {
        margin: 15px 0;
    }
    .location-info {
        text-align: center;
        font-size: 0.8rem;
        color: #a78bfa;
        margin-top: 5px;
    }
    
    /* Interesi - majhni */
    .interests-tags {
        display: flex;
        flex-wrap: wrap;
        gap: 6px;
        margin-top: 10px;
    }
    .interest-tag {
        background: linear-gradient(135deg, #818cf8, #c084fc);
        padding: 3px 10px;
        border-radius: 20px;
        font-size: 0.7rem;
        color: white;
        display: inline-block;
    }
    
    /* IDENTIČEN KOLEDAR KOT V REGISTRACIJI */
    .calendar-grid {
        display: grid;
        grid-template-columns: 90px repeat(7, 1fr);
        gap: 0.5rem;
        margin-top: 0.8rem;
        background: #1e293b;
        padding: 1rem;
        border-radius: 20px;
        overflow-x: auto;
    }
    
    .calendar-header {
        font-weight: bold;
        text-align: center;
        padding: 0.5rem;
        color: #a78bfa;
    }
    
    .day-header {
        background: rgba(129, 140, 248, 0.1);
        border-radius: 12px;
        padding: 8px 4px;
    }
    
    .time-label {
        font-weight: 600;
        padding: 0.5rem;
        color: #cbd5e1;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        font-size: 0.85rem;
        background: rgba(129, 140, 248, 0.05);
        border-radius: 12px;
    }
    
    .time-range {
        font-size: 0.7rem;
        color: #94a3b8;
        font-weight: normal;
    }
    
    .calendar-slot-display {
        background: #0f172a;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 50px;
        border: 1px solid #334155;
        transition: all 0.2s ease;
        padding: 0.5rem;
        text-align: center;
        font-size: 0.7rem;
        color: #94a3b8;
    }
    
    .calendar-slot-display.selected-slot {
        background: linear-gradient(135deg, #818cf8, #c084fc);
        border-color: #a78bfa;
        color: white;
    }
    
    .slot-time {
        font-size: 0.7rem;
    }
    
    .calendar-slot-display.selected-slot .slot-time {
        color: white;
        font-weight: 500;
    }
    
    .calendar-note {
        font-size: 0.7rem;
        margin-top: 0.8rem;
        color: #64748b;
        text-align: center;
    }
    
    /* Panel styles */
    .panel {
        background: rgba(15, 23, 42, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 28px;
        padding: 1.5rem;
        margin-bottom: 1rem;
        border: 1px solid rgba(129, 140, 248, 0.2);
    }
    .btn-full {
        width: 100%;
        margin-top: 10px;
    }
    
    /* Group card styles */
    .group-badge {
        background: linear-gradient(135deg, #818cf8, #c084fc);
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.8rem;
        color: white;
    }
    .match-badge {
        background: rgba(16, 185, 129, 0.2);
        color: #34d399;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.7rem;
    }
    .confirmed-badge {
        background: rgba(16, 185, 129, 0.2);
        color: #34d399;
        padding: 4px 12px;
        border-radius: 20px;
        font-size: 0.7rem;
    }
    .member-chip {
        background: #1e293b;
        padding: 3px 10px;
        border-radius: 20px;
        font-size: 0.7rem;
        color: #cbd5e1;
        display: inline-block;
        margin: 2px;
    }
    .participant-name {
        background: #1e293b;
        padding: 2px 8px;
        border-radius: 16px;
        font-size: 0.65rem;
        display: inline-block;
        margin: 2px;
        color: #cbd5e1;
    }
    .group-card {
        background: rgba(15, 23, 42, 0.95);
        backdrop-filter: blur(10px);
        border-radius: 28px;
        padding: 1.5rem;
        transition: all 0.3s ease;
        border: 1px solid rgba(129, 140, 248, 0.2);
        margin-bottom: 1rem;
    }
    .group-card:hover {
        transform: translateY(-2px);
        border-color: rgba(129, 140, 248, 0.4);
    }
    .group-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 1rem;
        flex-wrap: wrap;
        gap: 0.5rem;
    }
    .group-members {
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
        margin-bottom: 1rem;
    }
    .group-details {
        display: flex;
        gap: 1rem;
        margin-bottom: 0.5rem;
        font-size: 0.8rem;
        color: #94a3b8;
        flex-wrap: wrap;
    }
    .group-details i {
        color: #a78bfa;
        margin-right: 4px;
    }
    .group-actions {
        display: flex;
        gap: 12px;
        margin-top: 1rem;
        flex-wrap: wrap;
    }
    
    /* Search status */
    .search-status {
        background: #1e293b;
        padding: 8px 12px;
        border-radius: 40px;
        font-size: 0.75rem;
        text-align: center;
        margin: 10px 0;
        color: #94a3b8;
    }
    
    /* Dashboard grid */
    .dashboard-grid {
        display: grid;
        grid-template-columns: 380px 1fr;
        gap: 1.5rem;
        animation: slideUp 0.6s ease;
    }
    
    @media (max-width: 900px) {
        .dashboard-grid {
            grid-template-columns: 1fr;
        }
        .calendar-grid {
            grid-template-columns: 70px repeat(7, 1fr);
            font-size: 0.7rem;
        }
    }
    
    /* Gumbi */
    .btn-primary {
        background: linear-gradient(135deg, #818cf8, #c084fc);
        color: white;
        border: none;
        padding: 10px 20px;
        border-radius: 40px;
        font-weight: 600;
        font-size: 0.8rem;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    .btn-secondary {
        background: rgba(51, 65, 85, 0.8);
        color: #f1f5f9;
        border: 1px solid #475569;
        padding: 10px 20px;
        border-radius: 40px;
        font-weight: 600;
        font-size: 0.8rem;
        cursor: pointer;
        transition: all 0.3s ease;
    }
    .btn-primary:hover, .btn-secondary:hover {
        transform: translateY(-2px);
    }
</style>